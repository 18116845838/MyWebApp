//写一段代码，能根据一起帮用户的帮帮点（bCredit）输出他对应的等级（可适度精简）
/*var bCredit = NaN;
var grade;
if (isNaN(bCredit)) {
    grade = "错误";
} else {
    if (bCredit <= 10) {
        grade = "入门";
    } else if (bCredit <= 20) {
        grade = "聚气";

    } else if (bCredit <= 30) {
        grade = "凝水";

    } else if (bCredit <= 50) {
        grade = "萌芽";

    } else {
        grade = "单叶"
    }
}
console.log(grade);*/
//用switch表示一起帮身份代号
/*var user;
var rank;
switch (user) {
    case 0:
        rank = "访客";
        break;
    case 1:
        rank = "注册用户";
        break;
    case 2:
        rank = "文章发布者";
        break;
    case 3:
        rank = "管理员";
        break;
    case 4:
        rank = "超级管理员";
        break;
    default:
        rank = "身份有误";
        break;
}
console.log(rank);*/
